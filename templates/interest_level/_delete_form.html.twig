<form method="post" action="{{ path('app_interest_level_delete', {'id': interest_level.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ interest_level.id) }}">
    <button class="btn">Delete</button>
</form>
