{% extends 'base.html.twig' %}

{% block title %}InterestLevel{% endblock %}

{% block body %}
    <h1>InterestLevel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ interest_level.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ interest_level.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ interest_level.status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_interest_level_index') }}">back to list</a>

    <a href="{{ path('app_interest_level_edit', {'id': interest_level.id}) }}">edit</a>

    {{ include('interest_level/_delete_form.html.twig') }}
{% endblock %}
