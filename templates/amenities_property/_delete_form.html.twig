<form method="post" action="{{ path('app_amenities_property_delete', {'id': amenities_property.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ amenities_property.id) }}">
    <button class="btn">Delete</button>
</form>
