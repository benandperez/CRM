{% extends 'base.html.twig' %}

{% block title %}AmenitiesProperty{% endblock %}

{% block body %}
    <h1>AmenitiesProperty</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ amenities_property.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ amenities_property.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ amenities_property.status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_amenities_property_index') }}">back to list</a>

    <a href="{{ path('app_amenities_property_edit', {'id': amenities_property.id}) }}">edit</a>

    {{ include('amenities_property/_delete_form.html.twig') }}
{% endblock %}
