{% extends 'base.html.twig' %}

{% block title %}AdvertisingMedia{% endblock %}

{% block body %}
    <h1>AdvertisingMedia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ advertising_media.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ advertising_media.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ advertising_media.status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_advertising_media_index') }}">back to list</a>

    <a href="{{ path('app_advertising_media_edit', {'id': advertising_media.id}) }}">edit</a>

    {{ include('advertising_media/_delete_form.html.twig') }}
{% endblock %}
