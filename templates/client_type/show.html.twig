{% extends 'base.html.twig' %}

{% block title %}ClientType{% endblock %}

{% block body %}
    <h1>ClientType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ client_type.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ client_type.status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_client_type_index') }}">back to list</a>

    <a href="{{ path('app_client_type_edit', {'id': client_type.id}) }}">edit</a>

    {{ include('client_type/_delete_form.html.twig') }}
{% endblock %}
