{% extends 'base.html.twig' %}

{% block title %}ClientType index{% endblock %}

{% block body %}
    <h1>ClientType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Status</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for client_type in client_types %}
            <tr>
                <td>{{ client_type.id }}</td>
                <td>{{ client_type.name }}</td>
                <td>{{ client_type.status ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('app_client_type_show', {'id': client_type.id}) }}">show</a>
                    <a href="{{ path('app_client_type_edit', {'id': client_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_client_type_new') }}">Create new</a>
{% endblock %}
