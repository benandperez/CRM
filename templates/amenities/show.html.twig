{% extends 'base.html.twig' %}

{% block title %}Amenities{% endblock %}

{% block body %}
    <h1>Amenities</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ amenity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ amenity.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ amenity.status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_amenities_index') }}">back to list</a>

    <a href="{{ path('app_amenities_edit', {'id': amenity.id}) }}">edit</a>

    {{ include('amenities/_delete_form.html.twig') }}
{% endblock %}
