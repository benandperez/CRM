{% extends 'base.html.twig' %}

{% block title %}ProgrammedActivity{% endblock %}

{% block body %}
    <h1>ProgrammedActivity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ programmed_activity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ programmed_activity.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ programmed_activity.status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_programmed_activity_index') }}">back to list</a>

    <a href="{{ path('app_programmed_activity_edit', {'id': programmed_activity.id}) }}">edit</a>

    {{ include('programmed_activity/_delete_form.html.twig') }}
{% endblock %}
