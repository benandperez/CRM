<form method="post" action="{{ path('app_programmed_activity_delete', {'id': programmed_activity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ programmed_activity.id) }}">
    <button class="btn">Delete</button>
</form>
