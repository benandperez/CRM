{% extends 'base.html.twig' %}

{% block title %}TypeKitchen index{% endblock %}

{% block body %}
    <h1>TypeKitchen index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Status</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_kitchen in type_kitchens %}
            <tr>
                <td>{{ type_kitchen.id }}</td>
                <td>{{ type_kitchen.name }}</td>
                <td>{{ type_kitchen.status ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('app_type_kitchen_show', {'id': type_kitchen.id}) }}">show</a>
                    <a href="{{ path('app_type_kitchen_edit', {'id': type_kitchen.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_type_kitchen_new') }}">Create new</a>
{% endblock %}
