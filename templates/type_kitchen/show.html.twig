{% extends 'base.html.twig' %}

{% block title %}TypeKitchen{% endblock %}

{% block body %}
    <h1>TypeKitchen</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_kitchen.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_kitchen.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ type_kitchen.status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_kitchen_index') }}">back to list</a>

    <a href="{{ path('app_type_kitchen_edit', {'id': type_kitchen.id}) }}">edit</a>

    {{ include('type_kitchen/_delete_form.html.twig') }}
{% endblock %}
